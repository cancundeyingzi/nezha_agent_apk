# 真机验证清单

## 电池指标与授权路由（待真机验证）

- [ ] 普通模式下电量与系统一致，缺失温度/电流时不出现伪造的零值，且无提权请求。
- [ ] Shizuku 授权后上报正常；停止服务或撤权后回退普通电池采集，不出现 su 授权弹窗。
- [ ] Root 模式确认 UID 0 后读取；拒绝授权后持续采样不重复弹窗；关闭重开模式可以重新授权。
- [ ] 单电芯与双电芯设备分别比对 battery/uevent 和系统 API，确认没有重复节点相加或强制乘二。
- [ ] 双电芯厂商只暴露单电芯/等效参数时，计算功率显示“估算”，不宣称准确整包功率。
- [ ] 插拔充电器后充放电标签更新；满电/状态不明时显示“净功率”，不误标为整机功耗。
- [ ] 面板温度栏显示电量与功率，确认主题是否固定添加 ℃；必要时面板端按名称处理单位。

---

本次架构重构（`03faf24` → `8e2438d`）中，有若干改动**无法通过自动化测试覆盖**，需要在真机上确认。
按风险从高到低排列。完成一项就勾掉一项。

---

## P0：首次进入生产路径的代码

### 1. 文件管理器（TaskType 11）

`FileManager.kt` 等约 900 行此前一直被能力开关挡在门外，**从未在真机执行过**。
先在工具页开启「允许面板远程管理文件」。

- [ ] 面板打开文件管理，目录列表同时返回**文件和目录**
      （这正是 Manifest 申请 `MANAGE_EXTERNAL_STORAGE` 要解决的 FUSE 过滤问题；
      若只看到目录，说明「所有文件访问」未授权）
- [ ] 下载一个小文件
- [ ] 下载一个 >100 MB 的文件
- [ ] 上传文件
- [ ] 上传中途断网，重启探针后确认 `nezha_upload_*.tmp` 残留被清理
- [ ] 同时开 3 个文件会话，第 3 个被拒绝且提示可读
      （上限 `MAX_FILE_MANAGER_SESSIONS = 2`）

### 2. NAT 内网穿透（TaskType 9）

同样从未执行过。先开启「允许面板内网穿透」。

- [ ] 转发到一个内网 HTTP 服务，双向连通
- [ ] 目标地址不可达时，会话正常关闭而非挂死
- [ ] 传输过程中关闭开关，**新**请求被拒绝
      （已建立的会话不受影响，这是设计如此，非缺陷）

---

## P1：改动了判定来源

### 3. Root 模式下的授权判定

`AgentCommandHandler` 与 `FileManager` 原本读**持久化的** root 开关，
现改为读 `RootShell.isAuthorized()`（**实际生效的**授权）。
分析表明两者应始终一致，但这是本次唯一改动判定来源的地方。

在 Root/Shizuku 模式**开启**状态下：

- [ ] 文件管理：目录列表（走 `ls` 命令分支）
- [ ] 文件管理：文件移动
- [ ] 文件管理：文件读取（走流式命令分支）
- [ ] `@agent` 命令：用户应用进程查询
- [ ] `@agent` 命令：截图的高权限兜底（无障碍不可用时）

在 Root 模式**关闭**状态下，确认上述功能降级行为与改动前一致。

---

## P2：行为改进的可观察面

### 4. 启动失败退避重试

填一个**不可达**的服务端地址后启动探针：

- [ ] 通知栏依次显示「启动失败，5 秒后重试」→ 10 → 20 → 40 → 60 秒
- [ ] 重试期间按「启动探针」，立即重试且退避**重置回 5 秒**
- [ ] 填**格式非法**的配置（如空密钥）时，服务直接停止而非重试
      （配置问题需要用户修，重试无意义）

### 5. 能力开关的即时生效

- [ ] 探针运行中关闭「允许面板远程执行命令」，面板下发的**新**命令立即被拒
- [ ] 关闭过程中探针**不断线**（连接状态保持「已连接」）
- [ ] 文件管理、内网穿透两个开关同样即时生效

### 6. 悬浮窗保活

- [ ] 开启悬浮窗保活后停止探针，悬浮窗**被正确移除**
      （修复前会因线程错误而永久残留）
- [ ] 重载配置时，悬浮窗短暂消失后重新出现属正常
      （保活资源随运行时生命周期，不上报时无需保活）

### 7. 服务停止不再 ANR

- [ ] 建立若干 NAT/文件会话后停止探针，**不出现「应用无响应」弹窗**

---

## P3：UI 重构后的走查

`MainActivity.kt` 从 1811 行拆为 4 个文件，且 Toast 改为经事件通道投递。
拆分本身行为等价，但**无自动化测试保护**，需逐屏走查。

- [ ] 配置页：所有输入框、开关、按钮正常
- [ ] 工具页：所有开关、权限状态行、系统设置跳转正常
- [ ] 底部导航切换正常
- [ ] 各类提示（保存成功/失败、配置解析、模拟器状态）**都能弹出**
      （Toast 改为经 `Channel` 投递，时机由同步变为异步）
- [ ] 旋转屏幕后状态不丢失
- [ ] **Shizuku 授权竞态**：开启 Root 模式弹出 Shizuku 授权后立即旋转屏幕，
      授权结果仍能被界面接收（修复前此窗口内的结果会丢失）

---

## P3：Android 15/16 升级

`targetSdk` 34 → 35。本项目不上架 Google Play，因此 `specialUse` 前台服务类型
无需提交 Play Console 用途声明。

- [ ] 内容**未被状态栏遮挡**（曾在 Android 16 上复现：标题跑到状态栏时间下面）
- [ ] 有挖孔/刘海的设备上，横屏时内容未被摄像头区域裁切
- [ ] 中文文字行距、卡片高度正常
      （`elegantTextHeight` 在 targetSdk 35 起默认开启，对 CJK 字体影响比拉丁字体明显）
- [ ] 前台服务通知正常显示，长时间运行不被系统终止

---

## P0：并发修复（`649b8fc` → `c0f1be8`）

这几项改的是并发语义，单元测试只能覆盖其中一部分，其余必须真机确认。

### 1. Root 模式下的大文件传输不再自毁

修复前：上传走共享 shell 并独占 120 秒 → 状态流停摆 → 面板 10 秒回执超时 → 连接被拆
→ 传输被自己杀掉。现在长命令走 `RootShell.executeIsolated` 的专用进程。

开启 Root/Shizuku + 「允许面板远程管理文件」后：

- [ ] 上传一个 **>100 MB** 的文件，全程**面板不掉线**、状态曲线不中断
- [ ] 传输期间面板上的 CPU/内存曲线**持续更新**（这是本次修复的核心观察点）
- [ ] `@agent apps processes`（两次 dumpsys，约数秒）执行期间同样不中断状态上报
- [ ] `@agent screenshot` 在大屏设备上执行期间同样不中断

### 2. 共享 shell 超时收紧到 5 秒

`DEFAULT_SHELL_TIMEOUT_MS` 15s → 5s。理论上足够 `/proc` 读取，但 `dumpsys gpu` /
`dumpsys SurfaceFlinger` 在低端或高负载设备上可能更慢。

- [ ] 低端设备上 **GPU 使用率仍能采到**（若持续为 N/A，看日志有无「执行命令失败」）
- [ ] 进程数、TCP/UDP 连接数、负载均正常

### 3. 悬浮窗不再泄漏

修复前：清理超时会取消悬浮窗移除并丢弃句柄，每次重载泄漏一个 1×1 窗口且无法再移除。

开启「像素级透明悬浮窗」后：

- [ ] 反复「停止探针 → 启动探针」**10 次以上**，用
      `adb shell dumpsys window windows | grep -c nezha` 确认窗口数**不随次数增长**
- [ ] 在设备卡顿时（可同时跑重负载应用）重复上述操作，结论相同

### 4. 保活资源逐项超时

总预算 750ms 现由四个资源均分（各约 187ms）。悬浮窗被排到第一位。

- [ ] 停止探针后，音频保活确实停止（无残留静音播放占用）
- [ ] WakeLock 被释放（`adb shell dumpsys power | grep -i nezha` 无残留）
- [ ] 若日志出现「timed out after 187ms」，记录是哪个资源——可能需要上调总预算

### 5. 冷启动与开机自启不再卡主线程

配置初始化（含旧加密存储迁移、Keystore 建密钥）移出主线程。

- [ ] **从旧版本升级安装**后首次冷启动，无 ANR、无白屏超过 1 秒
- [ ] 重启设备后自启动仍然生效（`BootReceiver` 改为 `goAsync` + 后台线程）
- [ ] 启动后立刻查看「即时测试」，Root 授权状态最终正确
      （授权应用是异步的，短暂显示未授权属正常，但不应停留在未授权）

### 6. GeoIP 不再拖慢连接

- [ ] 断网状态下启动探针，**5 秒内**进入重连状态而非长时间无响应
- [ ] 正常网络下 GeoIP 信息仍能上报到面板

---

## 未做的事

- **instrumented 测试**：`androidTest` 目录仍为空，相关依赖已一并移除，
  待第一个真正的 UI 测试出现时再加回。
- **`DiskCollector` 之外的采集器**：仍与 Android 强耦合，其 Android 依赖是本质的
  （如 `GpuCollector` 全部依赖 `android.opengl.*`），未做接口化。
- **`AgentRuntime` / `RootShell` / `MainViewModel` 的测试**：三者仍零覆盖。
  `MainViewModel` 的剪贴板解析已抽成纯函数并补测，其余部分需先解开对
  `Application`、静态单例的依赖。
