# 真机验证操作手册

配套 `DEVICE_VERIFICATION.md`（**验什么**），本文写**怎么验**。
每项给出：操作步骤 → 通过标准 → 失败长什么样 → 失败时抓什么。

---

## 〇、准备工作

### 需要的东西

| | 用途 |
|:--|:--|
| Android 设备（真机或模拟器） | 被测端 |
| 一个可用的哪吒面板 | 下发任务、观察上报 |
| `adb` | 看日志、查服务状态 |
| 一台内网机器能跑 HTTP 服务 | 只有 NAT 验证需要 |
| Shizuku 或 Root | 只有 P1 需要 |

### 安装

```bash
cd <项目根目录>
./gradlew assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

### 开一个日志窗口，全程挂着

```bash
adb logcat -c                      # 先清空
adb logcat -s NezhaAgent:V         # 只看本应用的日志
```

> 应用所有日志的 TAG 都是 `NezhaAgent`。日志同时也在**应用内的日志页**可见，
> 手机上不方便接电脑时可以看那里。

### 常用观察命令

```bash
# 服务是否在运行、前台类型是什么
adb shell dumpsys activity services com.nezhahq.agent

# 看通知栏当前文案
adb shell dumpsys notification --noredact | grep -A3 "Nezha Agent"

# 悬浮窗是否还挂着（验证第 6 项用）
adb shell dumpsys window windows | grep -i nezha

# 应用私有目录（debug 包可用 run-as）
adb shell run-as com.nezhahq.agent ls -l cache/
adb shell run-as com.nezhahq.agent ls -l shared_prefs/
```

---

## P0-1 文件管理器（TaskType 11）

> ⚠️ 这段代码**从未在真机执行过**。请优先验证，且**不要在存有重要数据的设备上做删除类操作**。

### 前置

1. 应用内「工具」页 → 打开 **「允许面板远程管理文件」**
2. 系统设置 → 应用 → 哪吒探针 → 权限 → **「所有文件访问」授予**
   （不授予的话第 1 项必然失败，那是权限问题不是代码问题）

### 1.1 目录列表返回文件+目录

**操作**：面板 → 该设备 → 文件管理 → 打开 `/sdcard/`

**通过**：既能看到文件夹（Download、DCIM…）**也能看到文件**

**失败长这样**：只有文件夹、没有任何文件
→ 这正是 Scoped Storage 的 FUSE 过滤行为，说明「所有文件访问」没生效

**抓取**：
```bash
adb shell appops get com.nezhahq.agent MANAGE_EXTERNAL_STORAGE
```

### 1.2 下载小文件

**操作**：先造一个测试文件，再从面板下载
```bash
adb shell "echo hello-nezha > /sdcard/Download/test-small.txt"
```

**通过**：面板成功下载，内容为 `hello-nezha`

### 1.3 下载大文件（>100 MB）

**操作**：
```bash
adb shell "dd if=/dev/zero of=/sdcard/Download/test-big.bin bs=1M count=120"
```
从面板下载它。

**通过**：完整下载，大小 120 MB，传输过程中探针**不掉线**

**失败长这样**：传到一半卡死 / 探针掉线重连 / OOM 崩溃

**抓取**：
```bash
adb logcat -s NezhaAgent:V | grep -i "文件管理"
adb shell dumpsys meminfo com.nezhahq.agent | head -20
```

### 1.4 上传文件

**操作**：面板 → 上传任意文件到 `/sdcard/Download/`

**通过**：设备上出现该文件，内容一致
```bash
adb shell ls -l /sdcard/Download/
```

### 1.5 上传中断的残留清理

**操作**：
1. 面板发起一个**大文件**上传
2. 传到一半时**关闭设备网络**（飞行模式）
3. 检查残留：
   ```bash
   adb shell run-as com.nezhahq.agent ls -l cache/ | grep nezha_upload_
   ```
   此时应能看到 `nezha_upload_*.tmp`
4. 恢复网络 → 停止探针 → **重新启动探针**
5. 再查一次上面的命令

**通过**：重启后 `nezha_upload_*.tmp` **消失**

**说明**：清理发生在运行时启动时（`cleanupStaleUploads`），所以必须**重启探针**才会触发，
仅恢复网络不会清。

### 1.6 会话数上限

**操作**：面板上**同时**打开 3 个文件管理会话

**通过**：第 3 个被拒绝，提示中包含
> `The device has reached the limit of 2 active sessions for task type 11.`

**说明**：上限 `MAX_FILE_MANAGER_SESSIONS = 2`。

---

## P0-2 NAT 内网穿透（TaskType 9）

### 前置

1. 「工具」页 → 打开 **「允许面板内网穿透」**
2. 在**与手机同一内网**的机器上起一个 HTTP 服务：
   ```bash
   # 在内网机器上（假设 IP 是 192.168.1.100）
   python3 -m http.server 8000
   ```
3. 先确认手机能访问它（手机浏览器打开 `http://192.168.1.100:8000`）

### 2.1 双向连通

**操作**：面板 → 该设备 → NAT/内网穿透 → 目标填 `192.168.1.100:8000`
→ 通过面板给出的入口访问

**通过**：能看到该 HTTP 服务的目录列表，页面内容完整

**失败长这样**：连接建立但无数据 / 数据截断 / 立即断开

**抓取**：
```bash
adb logcat -s NezhaAgent:V | grep -i "NatManager"
```

### 2.2 目标不可达时不挂死

**操作**：目标填一个**不存在**的地址，如 `192.168.1.254:9999`

**通过**：会话在若干秒内**正常关闭**并有错误提示

**失败长这样**：面板一直转圈、会话永不释放、后续 NAT 请求也失败
（说明会话泄漏，`MAX_NAT_SESSIONS = 32` 会被逐渐耗尽）

**验证会话是否释放**：重复此操作 3 次后，再做一次 2.1 应仍然成功。

### 2.3 传输中关闭开关

**操作**：
1. 建立一个 NAT 会话并保持流量（如浏览器持续加载）
2. 应用内**关闭**「允许面板内网穿透」
3. 面板发起**新的** NAT 请求

**通过**：
- **新**请求被拒绝，提示 `Remote NAT is disabled on Android Agent.`
- **已建立**的会话继续工作（这是设计如此，非缺陷）

---

## P1 Root 授权判定来源的改动

> 本次唯一改动了判定来源的地方：原本读**持久化的** root 开关，
> 现改为读 `RootShell.isAuthorized()`（**实际生效的**授权）。
> 分析表明两者始终一致，此处是确认这个分析。

### 前置

打开 Root/Shizuku 模式并确认已授权：应用内 Shizuku 状态显示 **`✅ Shizuku 已授权`**。

也可用应用内「即时测试」确认：
> `▸ Shell 会话: su`（或 `shizuku`）
> `▸ Shell 存活: ✅`

### Root 模式**开启**时逐项确认

| # | 操作 | 通过标准 |
|:--|:--|:--|
| 3.1 | 文件管理打开 `/data/`（普通权限看不到的目录） | 能列出内容（走 `ls` 命令分支） |
| 3.2 | 文件管理移动一个文件到 `/data/local/tmp/` | 移动成功 |
| 3.3 | 文件管理下载 `/data/local/tmp/` 下的文件 | 下载成功（走流式命令分支） |
| 3.4 | 面板终端执行 `@agent apps` | 返回**用户应用**进程列表，而非"未开启 Root 模式"提示 |
| 3.5 | 面板终端执行 `@agent screenshot` | 无障碍不可用时能通过 `screencap` 兜底成功 |

### Root 模式**关闭**时确认降级

| # | 操作 | 通过标准 |
|:--|:--|:--|
| 3.6 | 重复 3.1 | 优雅降级（列不出 `/data/` 但不崩溃） |
| 3.7 | 重复 3.4 | 返回提示「跳过用户应用进程查询，Root/Shizuku 模式未开启」 |

**失败长这样**：Root 已授权但 3.4 却返回"未开启"提示
→ 说明两个判定源不一致，正是本项要抓的问题

---

## P2-4 启动失败退避重试

### 4.1 退避递进

**操作**：
1. 主页把服务端地址改成**不可达**的，如 `10.255.255.1`，端口 `5555`
2. 密钥随便填（**不能留空**，留空是配置错误走另一条路径）
3. 点「启动探针」
4. 盯住**通知栏**

**通过**：通知文案依次出现
```
启动失败，5 秒后重试
启动失败，10 秒后重试
启动失败，20 秒后重试
启动失败，40 秒后重试
启动失败，60 秒后重试
启动失败，60 秒后重试   ← 封顶后保持 60
```

**日志同步确认**：
```bash
adb logcat -s NezhaAgent:V | grep "秒后重试"
```

> 注意：不可达地址可能触发的是**连接**失败而非**启动**失败。
> 若通知显示的是「连接断开，正在重连...」，说明运行时已成功启动、走的是 gRPC 重连
> （固定 5 秒），那是另一条路径，不是本项。
> 要稳定触发启动失败，可用下面 4.3 的方式。

### 4.2 显式启动抢占退避

**操作**：在 4.1 退避到 40 或 60 秒时，点一次「启动探针」

**通过**：立即重试，且下一次失败的通知**回到「5 秒后重试」**

### 4.3 配置无效时不重试而是停止

**操作**：主页把**密钥清空**，点「启动探针」

**通过**：
- 弹出提示 `请先填写客户端密钥 (Secret)`，**服务根本不启动**

再试另一种：直接改坏存储里的配置
```bash
# 需要 root 或 debug 包
adb shell run-as com.nezhahq.agent ls shared_prefs/
```
若能构造出"存储可读但配置非法"的状态，通知应显示
> `连接配置无效或不可用，服务已停止`

且**不出现任何重试**——配置问题需要用户修，重试无意义。

---

## P2-5 能力开关即时生效

### 5.1 关闭远程命令后新请求立即被拒

**操作**：
1. 打开「允许面板远程执行命令」，启动探针，等待通知显示 **`已连接到面板`**
2. 面板下发一条命令（TaskType 4），确认能执行
3. **保持探针运行**，应用内**关闭**该开关
4. 面板**再下发**一条命令

**通过**：
- 第 4 步返回 `Remote shell execution is disabled on Android Agent.`
- **通知栏仍显示 `已连接到面板`**，探针**没有掉线**

**失败长这样**：
- 命令仍然执行 → 开关没生效（这是本次修复的核心缺陷）
- 探针断线重连 → 走成了完整重载，不符合设计

**日志确认**：
```bash
adb logcat -s NezhaAgent:V | grep -i "capabilit\|REFRESH"
```

### 5.2 另外两个开关同样即时

对「允许面板远程管理文件」（拒绝语 `Remote file manager is disabled on Android Agent.`）
和「允许面板内网穿透」（拒绝语 `Remote NAT is disabled on Android Agent.`）重复 5.1。

---

## P2-6 悬浮窗保活

### 6.1 停止探针后悬浮窗被移除

**操作**：
1. 授予悬浮窗权限，「工具」页打开**悬浮窗保活**
2. 启动探针
3. 确认悬浮窗已添加：
   ```bash
   adb shell dumpsys window windows | grep -i nezha
   ```
   应能看到一条窗口记录
4. **停止探针**
5. 再执行一次上面的命令

**通过**：第 5 步**查不到该窗口**

**失败长这样**：停止后窗口仍在
→ 这正是修复前的行为（在 IO 线程调 `removeView` 抛异常被吞掉）

**日志确认**：应出现 `OverlayKeepAlive: 悬浮窗已移除`，
而**不是** `OverlayKeepAlive: 移除悬浮窗失败`。

### 6.2 重载期间短暂消失属正常

**操作**：探针运行中，主页点「启动探针」触发重载

**通过**：悬浮窗短暂消失后重新出现

**说明**：保活资源随运行时生命周期，重载期间不上报也就无需保活。这不是缺陷。

---

## P2-7 停止服务不再 ANR

**操作**：
1. 启动探针并连上面板
2. 建立**多个**流式会话：开 2 个文件管理 + 若干 NAT 会话，保持流量
3. **在会话活跃时**点「停止探针」

**通过**：
- **不出现「应用无响应」弹窗**
- 界面立即响应，能继续操作

**失败长这样**：界面卡住数秒到十几秒，或弹出 ANR 对话框

**抓取（若失败）**：
```bash
adb shell ls -l /data/anr/
adb pull /data/anr/anr_<最新那个> ./anr-trace.txt
```
重点看主线程栈里是否有 `runBlocking` / `cancelAndJoin`。

---

## P3-8 UI 走查

> `MainActivity.kt` 从 1811 行拆成 4 个文件，Toast 改为经事件通道投递。
> **这部分没有任何自动化测试保护**，只能靠走查。

### 8.1 逐屏点一遍

**配置页**：每个输入框能输入、TLS 开关、Root 模式开关、粘贴解析、启动/停止、即时测试、重置配置存储

**工具页**：音频保活、悬浮窗、VPN 兼容、开机自启、三个远程能力开关、每一行权限状态的跳转按钮

**导航**：底部栏切换

### 8.2 提示语都能弹出（重点）

Toast 改为经 `Channel` 异步投递，**时机由同步变成异步**。逐一确认这些提示仍会出现：

| 触发 | 期望提示 |
|:--|:--|
| 粘贴板解析配置 | `配置已解析完成` |
| 剪贴板为空时解析 | `剪贴板中没有可解析的安装脚本` |
| 启动探针 | `后台探针服务已启动` |
| 停止探针 | `后台探针服务已停止` |
| 工具页任一开关 | `配置已保存，请在主页停止并重新启动探针以生效` |
| 重置配置存储 | `配置已重置，请重新填写连接信息` |
| 开启模拟器 | `娱乐模拟设备已开启` |

**失败长这样**：某个操作**完全没有提示**（事件丢失），或提示**延迟很久**才出现

### 8.3 旋转不丢状态

**操作**：填一半配置 → 旋转屏幕

**通过**：输入内容、开关状态、模拟器计数全部保留

### 8.4 Shizuku 授权竞态（重点）

> 这是验证本次竞态修复的**唯一**手段。

**操作**：
1. 确保 Shizuku 已运行但**尚未授权**本应用
   （可先在 Shizuku 应用里撤销本应用的授权）
2. 应用内打开 **Root 模式** 开关 → 弹出 Shizuku 授权对话框
3. 点「允许」后**立刻旋转屏幕**（越快越好）

**通过**：界面显示 `✅ Shizuku 已授权`，且弹出 `Shizuku 权限已授予，ADB Shell 模式可用`

**失败长这样**：授权明明点了「允许」，但界面**毫无反应**、状态仍是「等待 Shizuku 授权...」
→ 这正是修复前的行为（结果在 Activity 重建与首次组合之间丢失）

多试几次，这是时序窗口，单次成功不能完全排除问题。

---

## P3-9 Android 15 升级

> 需要一台 **Android 15（API 35）** 设备或模拟器。

### 9.1 edge-to-edge 无遮挡

> 此项**已在 Android 16 上复现过一次故障**：标题跑到了状态栏时间的下面。
> 根因是页面承载在嵌套的 `ComposeView` 里，那是独立的 composition 根，
> window insets 传不进去，`WindowInsets.statusBars` 读到 0 导致 padding 形同虚设。
> 已改为在外层读取 inset 后作为参数传入，请重点复验。

**操作**：在 Android 15/16 设备上逐屏浏览，并旋转屏幕各看一次

**通过**：顶部内容不被状态栏压住，底部导航栏不遮挡按钮

**失败长这样**：标题被状态栏切掉、与系统时间重叠，或底部按钮点不到

### 9.1b 挖孔/刘海设备

**操作**：在有挖孔或刘海的设备上**横屏**浏览

**通过**：内容未被摄像头区域裁切

**说明**：修复后用的是 `safeDrawing` 而非 `statusBars`，因此同时覆盖显示屏缺口。

### 9.1c 中文行距

**操作**：逐屏检查文字排版

**通过**：行距正常、卡片未被撑高、文字未异常换行

**说明**：`elegantTextHeight` 在 targetSdk 35 起默认为 true，会改变行高，
对 CJK 字体的影响比拉丁字体明显。

### 9.2 前台服务长时间运行

**操作**：启动探针后放置**数小时**（可过夜）

**通过**：
- 通知栏常驻，面板持续收到上报
- 服务未被系统终止

**中途检查**：
```bash
adb shell dumpsys activity services com.nezhahq.agent | grep -i "foreground\|isForeground"
```

---

## 附：失败时该抓什么

任何一项失败，请提供以下三样，我可以据此定位：

```bash
# 1. 完整日志（从复现开始）
adb logcat -c && <复现操作> && adb logcat -d -s NezhaAgent:V > nezha-log.txt

# 2. 服务状态
adb shell dumpsys activity services com.nezhahq.agent > service-dump.txt

# 3. 若是崩溃或 ANR
adb logcat -d -b crash > crash-log.txt
adb shell ls /data/anr/
```

外加一句话说明：**做了什么操作 → 期望什么 → 实际看到什么**。

---

## 附：快速回归（改动后重跑）

时间有限时，至少跑这四项——它们覆盖了本次风险最高的改动：

1. **P2-5.1** 关闭远程命令后新请求立即被拒（安全闸门，本次最高优先级修复）
2. **P2-7** 多会话下停止不 ANR（确定性崩溃的修复）
3. **P0-1.1** 文件管理目录列表（约 900 行首次上线）
4. **P3-8.2** 提示语都能弹出（事件通道改造的主要风险）
